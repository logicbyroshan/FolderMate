{
  "name": "@foldermate/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/index.js --external:node:sqlite",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@foldermate/config": "*",
    "@foldermate/shared": "*"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "esbuild": "^0.21.5",
    "typescript": "^5.6.3"
  }
}
